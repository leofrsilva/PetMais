import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:petmais/app/modules/home/submodules/pet_shop/submodules/graphics/pages/quantidade_por_estado/quantidade_por_estado_controller.dart';
import 'package:petmais/app/modules/home/submodules/pet_shop/submodules/graphics/graphics_module.dart';

void main() {
  initModule(GraphicsModule());
  // QuantidadePorEstadoController quantidadeporestado;
  //
  setUp(() {
    //     quantidadeporestado = GraphicsModule.to.get<QuantidadePorEstadoController>();
  });

  group('QuantidadePorEstadoController Test', () {
    //   test("First Test", () {
    //     expect(quantidadeporestado, isInstanceOf<QuantidadePorEstadoController>());
    //   });

    //   test("Set Value", () {
    //     expect(quantidadeporestado.value, equals(0));
    //     quantidadeporestado.increment();
    //     expect(quantidadeporestado.value, equals(1));
    //   });
  });
}
