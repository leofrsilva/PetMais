import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:petmais/app/modules/home/submodules/pet_shop/submodules/graphics/graphics_page.dart';

void main() {
  testWidgets('GraphicsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GraphicsPage(title: 'Graphics')));
    //  final titleFinder = find.text('Graphics');
    //  expect(titleFinder, findsOneWidget);
  });
}
