import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:petmais/app/modules/home/submodules/pet_shop/submodules/graphics/pages/types_pedidos/types_pedidos_page.dart';

void main() {
  testWidgets('TypesPedidosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TypesPedidosPage(title: 'TypesPedidos')));
    //  final titleFinder = find.text('TypesPedidos');
    //  expect(titleFinder, findsOneWidget);
  });
}
