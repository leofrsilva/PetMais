import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:petmais/app/modules/home/submodules/pet_shop/submodules/meus_produtos/meus_produtos_page.dart';

void main() {
  testWidgets('MeusProdutosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MeusProdutosPage(title: 'MeusProdutos')));
    //  final titleFinder = find.text('MeusProdutos');
    //  expect(titleFinder, findsOneWidget);
  });
}
