import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:petmais/app/modules/home/submodules/chat_petshop/chat_petshop_page.dart';

void main() {
  testWidgets('ChatPetshopPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatPetshopPage(title: 'ChatPetshop')));
    //  final titleFinder = find.text('ChatPetshop');
    //  expect(titleFinder, findsOneWidget);
  });
}
